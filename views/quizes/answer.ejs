<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Quiz</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h2>Quiz: el juego de las preguntas</h2>
    <p>
    <%= respuesta %>
      <% if(respuesta === 'Incorrecto') { %>
      <a href="/quizes/<%= quiz.id %>?fallos=<%= incremento %>">Volver a intentarlo</a>
      <% } else { %>
    <a href="/quizes/">Volver a las preguntas</a>
   <% } %>
    </p>
  </body>
</html>
